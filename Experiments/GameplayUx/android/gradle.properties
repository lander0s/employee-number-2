org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Kotlin's incremental compiler cannot close its caches on this machine - it
# fails `:audioplayers_android:compileDebugKotlin` with "Could not close
# incremental caches", which is a Windows file-locking problem rather than
# anything about the code. Turning incremental off costs rebuild time in a
# plugin nobody here edits.
kotlin.incremental=false
